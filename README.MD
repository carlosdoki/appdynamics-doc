## AppDynamics Document Configuration

1. `pip install -r requirements.txt` (or just install requests)
2. `python app-doc.py <host> <port> <user> <password> <account>`
3. Generate the Excel with documentation of customization 
